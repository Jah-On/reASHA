# reASHA
Simple application that automatically reconnect ASHA HA/CIs on boot and done streaming.

## Building
```
git clone https://github.com/Jah-On/reASHA
cd reASHA
cargo build # or `cargo run` to run 
```